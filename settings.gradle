pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "ListenerApp"
include ':app'
